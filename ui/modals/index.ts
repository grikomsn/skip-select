// https://saas-ui.dev/docs/components/overlay/modals-manager#custom-modals

import { createModals } from "@saas-ui/react";

export const { ModalsProvider, useModals } = createModals({
  modals: {
    //
  },
});
